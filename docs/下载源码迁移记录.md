# 下载代码来源与修改

2026-09-05，来源为用户指定的 `E:\点众\自动化工具\侵权巡检助手工作区\源码`：

| 原路径 | 本项目路径 |
|---|---|
| app/services/douyin_download_service.py | src/vendor/douyin.py |
| app/services/douyin_browser_probe.py | src/vendor/browser_probe.py |
| app/utils/failover.py | src/vendor/failover.py |

迁移脚本 `scripts/vendor_downloader.py` 只读取源码，不读取或复制凭证。调整了应用内部导入路径、日志入口和浏览器子进程启动命令，使浏览器子进程以 `python -m src.vendor.browser_probe` 启动。

下载配置由新适配器在内存中注入，输出改为当前项目临时目录，完整验证后迁移至批次下按视频 ID 命名的文件。原项目业务 UI、审核提示词、ASR 和其他平台下载均未引入。

图片模型调用参照 `E:\点众\YouTube字幕核验助手工作区\app\services\youtube_cover_review_service.py` 的端点拼接、Bearer 鉴权与 Base64 `image_url` 传图方式；本项目独立实现逐帧 OCR/logo 提示词、结果校验和批次管理。

两个原项目均未修改。浏览器兜底使用独立的 PySide6 内存 profile，不复用用户 Chrome 登录态。若抖音要求登录或验证码，本期保留失败记录供后续处理。
